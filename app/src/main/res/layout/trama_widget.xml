<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="14dp"
    android:paddingEnd="12dp"
    android:paddingBottom="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:letterSpacing="0.14"
                android:text="@string/widget_today_actions"
                android:textColor="@color/widget_amber"
                android:textSize="11sp"
                android:textStyle="bold" />
            <TextView
                android:id="@+id/widget_date"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/widget_preview_date"
                android:textColor="@color/widget_text"
                android:textSize="18sp"
                android:textStyle="bold" />
            <TextView
                android:id="@+id/widget_status"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/widget_status_idle"
                android:textColor="@color/widget_muted"
                android:textSize="11sp"
                android:textStyle="bold" />
            <Chronometer
                android:id="@+id/widget_recording_status"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/widget_red"
                android:textSize="11sp"
                android:textStyle="bold"
                android:visibility="gone" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_count"
            android:layout_width="wrap_content"
            android:layout_height="28dp"
            android:background="@drawable/widget_count_background"
            android:gravity="center"
            android:minWidth="72dp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/widget_zero_actions"
            android:textColor="@color/widget_muted"
            android:textSize="11sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/widget_refresh"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="3dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/widget_refresh"
            android:padding="8dp"
            android:src="@drawable/ic_widget_refresh"
            android:tint="@color/widget_muted" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="10dp"
        android:layout_weight="1"
        android:orientation="horizontal">

        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1">
            <ListView
                android:id="@+id/widget_action_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:divider="@android:color/transparent"
                android:dividerHeight="0dp"
                android:scrollbars="none" />
            <TextView
                android:id="@+id/widget_empty"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:padding="16dp"
                android:text="@string/widget_actions_empty"
                android:textColor="@color/widget_muted"
                android:textSize="13sp" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="54dp"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:gravity="center"
            android:orientation="vertical">
            <ImageView
                android:id="@+id/widget_device_action"
                style="@style/TramaWidgetFab"
                android:background="@drawable/widget_fab_watch_idle"
                android:contentDescription="@string/widget_device_action"
                android:src="@drawable/ic_widget_watch"
                android:tint="@color/widget_watch" />
            <ImageView
                android:id="@+id/widget_record_action"
                style="@style/TramaWidgetFab"
                android:layout_marginTop="10dp"
                android:background="@drawable/widget_fab_record_idle"
                android:contentDescription="@string/widget_start_recording"
                android:src="@drawable/ic_widget_record"
                android:tint="@color/widget_red" />
            <ImageView
                android:id="@+id/widget_listen_action"
                style="@style/TramaWidgetFab"
                android:layout_marginTop="10dp"
                android:background="@drawable/widget_fab_listen_idle"
                android:contentDescription="@string/widget_start_listening"
                android:src="@drawable/ic_widget_mic_off"
                android:tint="@color/widget_amber" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
